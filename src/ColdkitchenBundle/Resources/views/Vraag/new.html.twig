{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Vraag creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('vraag') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
