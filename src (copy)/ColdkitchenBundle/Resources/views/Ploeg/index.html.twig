{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ploeg list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ploegnaam</th>
                <th>Kapiteinnaam</th>
                <th>Kapiteinvoornaam</th>
                <th>Geboortedatum</th>
                <th>Straat</th>
                <th>Huisnr</th>
                <th>Bus</th>
                <th>Postcode</th>
                <th>Woonplaats</th>
                <th>Telefoon</th>
                <th>Gsm</th>
                <th>Emailadres</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ploeg_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.ploegnaam }}</td>
                <td>{{ entity.kapiteinNaam }}</td>
                <td>{{ entity.kapiteinVoornaam }}</td>
                <td>{% if entity.geboortedatum %}{{ entity.geboortedatum|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.straat }}</td>
                <td>{{ entity.huisnr }}</td>
                <td>{{ entity.bus }}</td>
                <td>{{ entity.postcode }}</td>
                <td>{{ entity.woonplaats }}</td>
                <td>{{ entity.telefoon }}</td>
                <td>{{ entity.gSM }}</td>
                <td>{{ entity.emailadres }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ploeg_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ploeg_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ploeg_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
