{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PartnerType</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ entity.naam }}</td>
            </tr>
            <tr>
                <th>Hoogte</th>
                <td>{{ entity.hoogte }}</td>
            </tr>
            <tr>
                <th>Breedte</th>
                <td>{{ entity.breedte }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('partnertype') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('partnertype_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
